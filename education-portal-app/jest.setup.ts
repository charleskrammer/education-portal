// jest.setup.ts — Runs after the test framework is installed.
// Imports @testing-library/jest-dom to extend expect() with DOM matchers
// such as toBeInTheDocument(), toHaveTextContent(), toBeDisabled(), etc.
import "@testing-library/jest-dom";
